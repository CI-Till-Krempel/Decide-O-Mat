# Release v1.3.2

**Decide-O-Mat v1.3.2** is a quick patch release to resolve a linting issue that caused deployment failures in v1.3.1.

### Fixed
- Resolved `process is not defined` lint error in the build configuration, ensuring smooth deployments.
